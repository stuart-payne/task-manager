export type DropdownOption = { id: number, name: string };

export type Task = {
	id: number,
	name: string,
	description: string,
	status: string,
	statusId: number,
	priority: string,
	priorityId: number,
	effort: number,
} 
